-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_runtime_credentials.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

CREATE TABLE IF NOT EXISTS CX_RUNTIME_CONTEXT_CREDENTIALS (EXECUTION_ID VARCHAR(128) PRIMARY KEY REFERENCES CX_RUNTIME_CONTEXT_BINDINGS(EXECUTION_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), TOKEN_DIGEST VARCHAR(64) NOT NULL REFERENCES CX_AGENT_ACCESS_TOKENS(TOKEN_DIGEST), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL);

CREATE OR REPLACE FUNCTION cx86_reject_history_mutation() RETURNS trigger LANGUAGE plpgsql AS $cx86$ BEGIN RAISE EXCEPTION 'Continuity identity and history are immutable' USING ERRCODE='23000'; END $cx86$;

REVOKE ALL ON FUNCTION cx86_reject_history_mutation() FROM PUBLIC;

CREATE OR REPLACE TRIGGER CX86_7DF6C58399D72FEA BEFORE DELETE OR UPDATE ON CX_RUNTIME_CONTEXT_CREDENTIALS FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_RUNTIME_CONTEXT_CREDENTIALS FROM PUBLIC, ai_agent_runtime;
