-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_continuity_bindings.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

CREATE TABLE IF NOT EXISTS CX_HANDOFF_EVIDENCE (HANDOFF_ID VARCHAR(128) NOT NULL, REVISION_NO INTEGER NOT NULL, ITEM_NO INTEGER NOT NULL CHECK(ITEM_NO>0), FAMILY VARCHAR(24) NOT NULL, ENTITY_ID VARCHAR(128) NOT NULL, SOURCE_REVISION_ID VARCHAR(128) NOT NULL, CONTENT_DIGEST VARCHAR(64) NOT NULL, PUBLICATION_ID VARCHAR(128) REFERENCES CX_CONTEXT_PUBLICATIONS(PUBLICATION_ID), PRIMARY KEY(HANDOFF_ID,REVISION_NO,ITEM_NO), UNIQUE(HANDOFF_ID,REVISION_NO,FAMILY,ENTITY_ID,SOURCE_REVISION_ID), FOREIGN KEY(HANDOFF_ID,REVISION_NO) REFERENCES CX_HANDOFF_REVISIONS(HANDOFF_ID,REVISION_NO));

CREATE TABLE IF NOT EXISTS CX_HANDOFF_INSTANCE_BINDINGS (HANDOFF_ID VARCHAR(128) NOT NULL REFERENCES CX_HANDOFFS(HANDOFF_ID), PARTICIPANT_SIDE VARCHAR(16) NOT NULL CHECK(PARTICIPANT_SIDE IN ('SENDER','RECIPIENT')), PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, PRIMARY KEY(HANDOFF_ID,PARTICIPANT_SIDE));

CREATE TABLE IF NOT EXISTS CX_HANDOFF_CREDENTIAL_USES (HANDOFF_ID VARCHAR(128) NOT NULL REFERENCES CX_HANDOFFS(HANDOFF_ID), RESOURCE_VERSION INTEGER NOT NULL CHECK(RESOURCE_VERSION>0), OPERATION VARCHAR(64) NOT NULL, PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), TOKEN_DIGEST VARCHAR(64) NOT NULL REFERENCES CX_AGENT_ACCESS_TOKENS(TOKEN_DIGEST), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, PRIMARY KEY(HANDOFF_ID,RESOURCE_VERSION));

CREATE OR REPLACE FUNCTION cx86_reject_history_mutation() RETURNS trigger LANGUAGE plpgsql AS $cx86$ BEGIN RAISE EXCEPTION 'Continuity identity and history are immutable' USING ERRCODE='23000'; END $cx86$;

REVOKE ALL ON FUNCTION cx86_reject_history_mutation() FROM PUBLIC;

CREATE OR REPLACE TRIGGER CX86_ACA2693B55713861 BEFORE DELETE OR UPDATE ON CX_HANDOFF_EVIDENCE FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_HANDOFF_EVIDENCE FROM PUBLIC, ai_agent_runtime;

CREATE OR REPLACE TRIGGER CX86_6C8211D0F7BD68A5 BEFORE DELETE OR UPDATE ON CX_HANDOFF_INSTANCE_BINDINGS FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_HANDOFF_INSTANCE_BINDINGS FROM PUBLIC, ai_agent_runtime;

CREATE OR REPLACE TRIGGER CX86_9D56E7F8080BD438 BEFORE DELETE OR UPDATE ON CX_HANDOFF_CREDENTIAL_USES FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_HANDOFF_CREDENTIAL_USES FROM PUBLIC, ai_agent_runtime;
