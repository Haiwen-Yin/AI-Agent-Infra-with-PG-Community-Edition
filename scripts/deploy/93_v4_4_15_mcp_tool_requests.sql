-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_mcp_requests.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

CREATE TABLE IF NOT EXISTS CX_MCP_TOOL_REQUESTS (REQUEST_ID VARCHAR(128) PRIMARY KEY, JOB_ID VARCHAR(128) NOT NULL UNIQUE REFERENCES EXECUTION_JOBS(JOB_ID), TOOL_ID VARCHAR(128) NOT NULL, PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), SECURITY_DOMAIN_ID VARCHAR(128) NOT NULL REFERENCES CX_SECURITY_DOMAINS(SECURITY_DOMAIN_ID), INSTANCE_ID VARCHAR(128) NOT NULL REFERENCES CX_AGENT_INSTANCES(INSTANCE_ID), TOKEN_DIGEST VARCHAR(64) NOT NULL REFERENCES CX_AGENT_ACCESS_TOKENS(TOKEN_DIGEST), FENCING_TOKEN INTEGER NOT NULL CHECK(FENCING_TOKEN>0), CONTRACT_DIGEST VARCHAR(64) NOT NULL, PAYLOAD_DIGEST VARCHAR(64) NOT NULL, REQUEST_DIGEST VARCHAR(64) NOT NULL, IDEMPOTENCY_KEY VARCHAR(128) NOT NULL, CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, UNIQUE(PRINCIPAL_ID,IDEMPOTENCY_KEY));

CREATE OR REPLACE FUNCTION cx86_reject_history_mutation() RETURNS trigger LANGUAGE plpgsql AS $cx86$ BEGIN RAISE EXCEPTION 'Continuity identity and history are immutable' USING ERRCODE='23000'; END $cx86$;

REVOKE ALL ON FUNCTION cx86_reject_history_mutation() FROM PUBLIC;

CREATE OR REPLACE TRIGGER CX86_36C6575FDD050E00 BEFORE DELETE OR UPDATE ON CX_MCP_TOOL_REQUESTS FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_MCP_TOOL_REQUESTS FROM PUBLIC, ai_agent_runtime;
