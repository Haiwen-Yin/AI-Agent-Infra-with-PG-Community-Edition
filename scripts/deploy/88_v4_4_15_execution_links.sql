-- v4.4.15 normalized continuity entities and immutable history.

-- Generated by tools/v415_generate_execution_links.py; do not edit independently.

-- End-user SQL is denied. Authenticated platform services enforce current resource authority.

CREATE TABLE IF NOT EXISTS CX_WORK_EXECUTION_LINKS (WORK_CONTRACT_ID VARCHAR(128) NOT NULL REFERENCES CX_WORK_CONTRACTS(WORK_CONTRACT_ID), LINK_KIND VARCHAR(16) NOT NULL CHECK(LINK_KIND IN ('WORKSPACE','TASK','GRAPH')), ENTITY_ID VARCHAR(128) NOT NULL, SOURCE_PRINCIPAL_ID VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CREATED_BY VARCHAR(128) NOT NULL REFERENCES CX_PRINCIPALS(PRINCIPAL_ID), CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL, PRIMARY KEY(WORK_CONTRACT_ID,LINK_KIND));

CREATE OR REPLACE FUNCTION cx86_reject_history_mutation() RETURNS trigger LANGUAGE plpgsql AS $cx86$ BEGIN RAISE EXCEPTION 'Continuity identity and history are immutable' USING ERRCODE='23000'; END $cx86$;

REVOKE ALL ON FUNCTION cx86_reject_history_mutation() FROM PUBLIC;

CREATE OR REPLACE TRIGGER CX86_FAB22E09ECF6F00A BEFORE DELETE OR UPDATE ON CX_WORK_EXECUTION_LINKS FOR EACH STATEMENT EXECUTE FUNCTION cx86_reject_history_mutation();

REVOKE ALL ON CX_WORK_EXECUTION_LINKS FROM PUBLIC, ai_agent_runtime;
